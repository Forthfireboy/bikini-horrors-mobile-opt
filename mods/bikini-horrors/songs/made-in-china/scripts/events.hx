
function postCreate() {
    WindowUtils.winTitle = window.title = "SHOP LIKE A BILLIONAIRE";
}

function stepHit(curStep:Int) {
    if (curStep == 1264) {
        WindowUtils.winTitle = window.title = "NOTHING IS REAL";
    }
}

function onSongEnd() {
    WindowUtils.winTitle = window.title = "BIKINI HORRORS";
}